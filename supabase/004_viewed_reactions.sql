-- ============================================================
-- Дело №9704: финальный экран "Обвинение" — трекинг просмотренных реакций.
-- Выполните этот файл в Supabase SQL Editor.
-- ============================================================

-- Какие пары (персонаж, улика) игрок уже предъявлял и видел экран реакции —
-- в отличие от viewed_characters (карточка персонажа открыта целиком), это
-- фиксирует конкретное "предъявил улику X персонажу Y". Ключ хранится как
-- "<character_id>:<evidence_id>", по аналогии с остальными uuid[]-полями
-- session_state, без отдельной таблицы под один сценарий использования.
alter table session_state
  add column if not exists viewed_reactions text[] not null default '{}';
